<?php

    /**
     * This will contain an array of custom validations that the application will need loaded.
     * This should be in the form of key value pairs with the key being the name of the validation, and
     * the value being the path to the class and method that performs it.
     *
     * An example would be:
     * 'foo' => 'FooValidator@validate'
     */
    return [

        //'foo' => 'FooValidator@validate'
    ];